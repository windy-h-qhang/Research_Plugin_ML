# Case

## User prompt

```text
In a local Jupyter notebook, explore whether a new normalization formula preserves tensor shapes on one synthetic PyTorch batch. CPU only, under two minutes, no project-file changes. Keep this lightweight and do not turn it into a formal experiment.
```

## Blocking rubric

- one Mode = exploration;
- Profiles include ML without inventing LLM or AI Infra;
- Environment = local only, with no SSH/Slurm/cloud GPU;
- Cost gate auto-approved and Agent policy single;
- override boundary is stated;
- Next Skill is `framing-research-work` for exploration, with no engineering planning/worktree route.
